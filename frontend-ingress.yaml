apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - "frontend.35.203.169.95.sslip.io"
    secretName: "frontend.35.203.169.95.sslip.io"
  rules:
  - host: "frontend.35.203.169.95.sslip.io"
    http:
      paths: 
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-lb
            port:
              number: 80
